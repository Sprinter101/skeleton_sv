{namespace sv.lSberVmeste.bPageManager.Template}

/**
 * @constructor
 * @extends cl.iContentManager.Template
 */
{template .pageManager}
    {call .init data="all"/}
{/template}

/**
 * @override
 * @param params {
 *     config: {
 *         state: ?string
 *     }
 * }
 */
{template .body}
    {call .initChildWithState}
        {param params: [
            'states': [
                [
                    'type': 'navPage3',
                ]
            ],
            'defaultControl': 'NavPage3',
            'currentState': $params?.config?.state ?: 'navPage3'
        ]/}
    {/call}
{/template}

/**
 * @override
 */
{template .rootClass}
    b-page-manager
{/template}
